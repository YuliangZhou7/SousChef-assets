{
  "version": "v1",
  "scope": "developer-local dogfooding (M1)",
  "description": "First M1 voice-asset definition. Scope is dev/dogfooding only — see hey-chef-wakeword.publish_eligible=false. The first public-release-eligible build is v2, after the custom-trained Hey Chef classifier replaces the openwakeword.com community model.",
  "assets": [
    {
      "id": "hey-chef-wakeword",
      "filename": "hey-chef.onnx",
      "upstream_url": "local:manual:openwakeword.com/library community section (specific URL not recorded; manual download)",
      "sha256": "TBD — populated by scripts/hash.sh after mirroring",
      "size_bytes": 0,
      "required_if": "always",
      "unpacked_path": "hey-chef.onnx",
      "publish_eligible": false,
      "notes": "DEV/DOGFOOD ONLY. Per openwakeword.com/terms (reviewed 2026-05-19) the community models cannot be redistributed in a public release. This entry stays in staging/v1/ for the developer's local builds only; scripts/publish.sh skips assets with publish_eligible=false. Custom-trained replacement lands in v2 before TestFlight."
    },
    {
      "id": "kokoro-82m-en",
      "filename": "kokoro-82m-en.tar.gz",
      "upstream_url": "local:scripts/build-kokoro-tarball.sh",
      "sha256": "TBD",
      "size_bytes": 0,
      "required_if": "always",
      "unpacked_path": "kokoro-82m-en/",
      "publish_eligible": true,
      "notes": "Option A locked (2026-05-19): tarball of the English subset of huggingface.co/FluidInference/kokoro-82m-coreml (Apache 2.0). Built locally via scripts/build-kokoro-tarball.sh v1 — requires huggingface-cli. The script downloads the subset listed in upstream/kokoro.md, records the source HF commit SHA in staging/v1/kokoro-82m-en.commit.txt, and tars to staging/v1/kokoro-82m-en.tar.gz. scripts/mirror.sh recognises the 'local:' upstream_url prefix and skips curl for this entry."
    }
  ],
  "removed_assets": [
    {
      "id": "whisper-large-v3-turbo",
      "removed_on": "2026-05-19",
      "reason": "Whisper dropped from M1 scope. ~1.6 GB asset + ~1.5–2 GB peak resident RAM not justified vs. iOS 26 SpeechAnalyzer / SFSpeechRecognizer (both OS-shipped). Reintroduced only via the conditional post-MVP 'Whisper open-form-STT quality experiment' milestone in SousChefApp-v2 docs/milestone-plan.md — if that ever runs, it ships its own asset version (not v1)."
    }
  ],
  "in_app_manifest_template": {
    "version": "v1",
    "open_form_stt_engine": "TBD — set after the M1 Q3 spike picks between iOS 26 SpeechAnalyzer and SFSpeechRecognizer (both OS-shipped; no asset to mirror)",
    "base_url": "https://github.com/YuliangZhou7/SousChef-assets/releases/download/v1",
    "assets_field_to_be_populated_by": "scripts/app-manifest.sh",
    "publish_eligibility_filter": "scripts/publish.sh and scripts/app-manifest.sh both filter out assets where publish_eligible=false"
  }
}
